# GENERATED by dgen from actor.dgen — do not edit.

from __future__ import annotations

from dataclasses import dataclass

from dgen import Block, Dialect, Op, Type, Value
from dgen.dialects.builtin import Nil
from dgen.dialects.index import Index

actor = Dialect("actor")

@dataclass(eq=False)
class ActorOp(Op):
    consume_rate: Value[Index]
    produce_rate: Value[Index]
    input: Value
    body: Block
    type: Type = Nil()

@dataclass(eq=False)
class ProduceOp(Op):
    value: Value
    type: Type = Nil()

@dataclass(eq=False)
class PipelineOp(Op):
    input: Value
    body: Block
    type: Type
