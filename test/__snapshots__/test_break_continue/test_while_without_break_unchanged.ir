import algebra
import goto
import index
import number
import record

%while_header0 : Nil = goto.region([index.Index(0)]) body<%self: goto.Label, %exit0: goto.Label>(%wh0_i: index.Index):
    %while_body0 : goto.Label = goto.label([]) body(%wb0_i: index.Index) captures(%self, %exit0):
        %one : index.Index = 1
        %next : index.Index = algebra.add(%wb0_i, %one)
        %carry : Tuple<[index.Index]> = record.pack([%next])
        %0 : Nil = goto.branch<%self>(%carry)

    %ten : index.Index = 10
    %cmp : number.Boolean = algebra.less_than(%wh0_i, %ten)
    %1 : Nil = goto.conditional_branch<%while_body0, %exit0>(%cmp, [%wh0_i], [])
