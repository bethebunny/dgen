import goto
import index
import record

%t : Tuple<[index.Index, index.Index]> = record.pack([index.Index(99), index.Index(1)])
%r : index.Index = goto.region(%t) body<%_: goto.Label, %r_exit: goto.Label>(%x: index.Index, %y: index.Index):
    %first : index.Index = chain(%x, %y)
