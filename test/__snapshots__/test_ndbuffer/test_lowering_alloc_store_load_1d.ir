# Lowered through: NDBufferToMemory
#
# Input IR:
#
#   import function
#   import index
#   import ndbuffer
#   import number
#
#   %main : function.Function<[number.Float64], number.Float64> = function.function<number.Float64>() body(%x: number.Float64):
#       %buf : ndbuffer.NDBuffer<ndbuffer.Shape<index.Index(1)>([3]), number.Float64> = ndbuffer.alloc(ndbuffer.Shape<index.Index(1)>([3]))
#       %st : Nil = ndbuffer.store(%buf, %x, %buf, [index.Index(0)])
#       %val : number.Float64 = ndbuffer.load(%st, %buf, [index.Index(0)])

import function
import index
import memory
import ndbuffer
import number
import record

%main : function.Function<[number.Float64], number.Float64> = function.function<number.Float64>() body(%x: number.Float64):
    %0 : memory.Reference<number.Float64> = memory.heap_allocate<number.Float64>(index.Index(3))
    %1 : ndbuffer.NDBuffer<ndbuffer.Shape<index.Index(1)>([3]), number.Float64> = record.pack([%0, index.Index(3)])
    %2 : memory.Reference<number.Float64> = record.get<index.Index(0)>(%1)
    %3 : memory.Reference<number.Float64> = memory.offset(%2, index.Index(0))
    %4 : Nil = memory.store(%1, %x, %3)
    %5 : memory.Reference<number.Float64> = record.get<index.Index(0)>(%1)
    %6 : memory.Reference<number.Float64> = memory.offset(%5, index.Index(0))
    %7 : number.Float64 = memory.load(%4, %6)
